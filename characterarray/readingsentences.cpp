#include <bits/stdc++.h>
using namespace std;
int main()
{
    int maxlen;
    cin >> maxlen;
    char delim;
    delim = cin.get();
    char a[maxlen];
    cin.getline(a, maxlen, delim);
    return 0;
}